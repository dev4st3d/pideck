# Fast continuous checks for every push and pull request.
# Independent jobs run concurrently and each is hard-capped at ten minutes.
name: Checks

on:
  push:
    branches: [master, main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: checks-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_PROFILE_DEV_DEBUG: 0
  CARGO_PROFILE_TEST_DEBUG: 0
  RUSTFLAGS: -D warnings

jobs:
  source:
    name: Format and bridge
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Format
        run: cargo fmt --all -- --check

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: "22"
          check-latest: true

      - name: Verify source package and palette contracts
        run: node scripts/verify-source.mjs

      - name: Test bridge
        shell: pwsh
        run: node --test (Get-ChildItem -Path bridge,scripts -Filter *.test.mjs).FullName

  clippy:
    name: Clippy
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: checks-clippy

      - name: Setup Node for exact font preparation
        uses: actions/setup-node@v7
        with:
          node-version: "22"
          check-latest: true

      - name: Prepare SHA-256-verified design fonts
        run: node scripts/prepare-fonts.mjs

      - name: Clippy
        run: cargo clippy --locked --all-targets --all-features -- -D warnings

  tests:
    name: Rust tests
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: checks-tests

      - name: Setup Node for bridge integration fixtures
        uses: actions/setup-node@v7
        with:
          node-version: "22"
          check-latest: true

      - name: Prepare SHA-256-verified design fonts
        run: node scripts/prepare-fonts.mjs

      - name: Check all targets
        run: cargo check --locked --all-targets

      - name: Test
        run: cargo test --locked --all-targets
